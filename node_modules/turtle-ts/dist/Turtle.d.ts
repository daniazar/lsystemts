import { Point } from "./Point";
export declare class Turtle {
    private _position;
    private _angle;
    private _color;
    private _context;
    private _width;
    private _penDown;
    constructor(context: CanvasRenderingContext2D, startingPosition?: Point);
    logPenStatus(): void;
    setPosition(position: Point): void;
    forward(distance: any): void;
    backward(distance: any): void;
    left(angleInDegrees: number): this;
    right(angleInDegrees: number): void;
    private radians;
    angle(): number;
    setColor(color: string): void;
    setWidth(width: number): void;
    penUp(): void;
    penDown(): void;
    position: Point;
}
